# Kopieer dit bestand naar ".env.local" en pas eventueel de waarde aan.
#
# Deze token moet de RuneLite plugin meesturen in het "apiKey" veld
# bij het posten van een nieuwe PB naar /api/pb.
PB_API_KEY=dev-token

# Auth.js Discord OAuth
AUTH_DISCORD_ID=
AUTH_DISCORD_SECRET=
AUTH_SECRET=
